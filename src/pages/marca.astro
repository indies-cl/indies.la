---
import "../global.css";
import i18n from "../i18n/config";
import Footer from "../components/Footer.astro";
import { ColorSwatch } from "../components/ColorSwatch";
import { CodeSnippet } from "../components/CodeSnippet";
import FontPreloads from "../components/FontPreloads.astro";
import { getLanguageFromPath } from "../utils/language";

const lang = getLanguageFromPath(Astro.url.pathname);
i18n.changeLanguage(lang);
const t = i18n.getFixedT(lang);

const tailwindConfigCode = `@theme {
  --color-*: initial;
  --color-bg: rgb(16, 15, 15);
  --color-bg-2: rgb(28, 27, 26);
  --color-ui: rgb(40, 39, 38);
  --color-ui-2: rgb(52, 51, 49);
  --color-ui-3: rgb(64, 62, 60);
  --color-tx-3: rgb(87, 86, 83);
  --color-tx-2: rgb(135, 133, 128);
  --color-tx: rgb(206, 205, 195);
  --color-og: oklch(0.66 0.18 45);

  --font-sans: 'DINdong', sans-serif;
  --font-serif: 'BIZUDPMincho', serif;
  --font-mono: 'VictorMono', monospace;
}`;

const codeAgentsCode = `## DESIGN SYSTEM

indies.cl appeals to people tired of the monotonous and asymmetric nature of corporations, our communication is simple, everything in lowercase, we avoid using "¡" or "¿", being as casual as possible

### SPACING

- USE spacing, we use \`-4\` (p-4).
- USE either use \`text-xl\` or \`text-2xl\`, no other values allowed.

### FONTS

- USE font-serif (BIZ UDPMincho) as default with \`text-xl\`. Both properities inheritet from \`text-xl\`. body text is lowercase (\`lowercase\` class) unlike headings.
- USE font-sans (DINdong) for titles and headings with \`text-2xl\`
- USE font-mono (Victor Mono) for anything engineering or AI related, snippets and terminals.

CONSIDER: these fonts have no weights other than 400. therefore \`font-bold\` is usable; differenctiate text via colors.

### colors

USE ONLY the following colors:

- \`bg\`: main background
- \`bg-2\`: secondary background
- \`ui\`: borders
- \`ui-2\`: hovered borders
- \`ui-3\`: active borders
- \`tx-2\`: faint text
- \`tx-2\`: muted text
- \`tx\`: primary text
- \`og\`: primary color, signals brand, is bright and attention grabbing`;
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <FontPreloads />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{t("branding.title")}</title>
  </head>
  <body class="bg-bg antialiased text-balance text-tx text-xl font-serif p-4">
    <div class="max-w-lg mx-auto space-y-4">
      <h1 class="text-2xl font-sans text-og normal-case">
        {t("branding.heading")}
      </h1>

      <p class="font-serif">
        {t("branding.communicationStyle")}
      </p>

      <h2 class="text-2xl font-sans text-og normal-case">
        {t("branding.fontsHeading")}
      </h2>

      <p class="font-serif">
        {t("branding.fonts.serif.before")}<a
          href="https://uncut.wtf/serif/biz-udpmincho/"
          class="text-og underline"
          target="_blank"
          rel="noopener noreferrer">{t("branding.fonts.serif.name")}</a
        >
      </p>
      <p class="font-sans text-2xl">
        {t("branding.fonts.sans.before")}<a
          href="https://typotheque.genderfluid.space/fr/fontes/dindong"
          class="text-og underline"
          target="_blank"
          rel="noopener noreferrer">{t("branding.fonts.sans.name")}</a
        >{t("branding.fonts.sans.after")}
      </p>
      <p class="font-mono">
        {t("branding.fonts.mono.before")}<a
          href="https://rubjo.github.io/victor-mono/"
          class="text-og underline"
          target="_blank"
          rel="noopener noreferrer">{t("branding.fonts.mono.name")}</a
        >
      </p>

      <h2 class="text-2xl font-sans text-og normal-case">
        {t("branding.colorsHeading")}
      </h2>

      <ColorSwatch client:load />

      <h3 class="text-2xl font-sans mt-4 text-og normal-case">
        {t("branding.tailwindConfigHeading")}
      </h3>

      <CodeSnippet client:load code={tailwindConfigCode} language="css" />

      <h3 class="text-2xl font-sans text-og normal-case">
        {t("branding.codeAgentsHeading")}
      </h3>

      <CodeSnippet client:load code={codeAgentsCode} language="markdown" />

      <Footer pathname={Astro.url.pathname} />
    </div>
  </body>
</html>
